from lib2to3.pytree import BasePattern
from sre_constants import CHARSET

from charset_normalizer import CharsetMatches


dbc.Row(
    dbc.NavbarSimple(
        [
            dbc.NavItem(dbc.NavLink(
                        "Charts", href="#", active="exact")),
            dbc.NavItem(dbc.NavLink(
                        "Backtest", href="#", active="exact")),
            dbc.NavItem(dbc.NavLink(
                        "DCA Guide", href="#", active="exact")),

        ],
    )
)


#### TODO ###
FIX FILE STRUCTURE FOR CHARTS
ADD ROI CHARTS
FIX NAV BACKGROUND COLOR 
TOTAL MARKETCAP CHARTS 
ALT COIN CHARTS
MAKE CHARTS FILL SCREEN BETTER
STYLE PAGE BETTER
ADD MORE TOOLS TO CHARTS (LOG/LINEAR)

